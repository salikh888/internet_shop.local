include header

div(class='wrap')
    div(class='content')
        header(class='site-header')
            include site-header

        div(class='container')
            div(class='row')
                div(class='col col-md-12')
                    h2 #{cat[0]['category']}
                each val, index in goods
                    include card
                div #{cat[0]['description']}